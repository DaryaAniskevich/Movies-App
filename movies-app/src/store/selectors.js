export const moviesSelector = (state) => state.movies.movies;
export const moviesIsLoadingSelector = (state) => state.movies.isLoading;

export const searchSelector = (state) => state.search.value;
export const searchBySelector = (state) => state.searchBy.searchBy;
